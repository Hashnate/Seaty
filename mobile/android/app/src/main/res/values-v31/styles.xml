<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- What the system splash is removed onto. Same drawable, same logo,
             same position, so MainActivity can drop the splash view instantly. -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowSplashScreenBackground">#ffffff</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/android12splash</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.
         
         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- See values/styles.xml — must not be ?android:colorBackground. -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <!-- LaunchTheme already opts out; NormalTheme must too. It is applied
             before Flutter's first frame, so without this the OS "force dark"
             setting (MIUI et al. apply it app-wide) inverts the white launch
             drawable and shows a dark logo screen before the real splash.
             Flutter renders to its own surface and is never inverted, which is
             why only this native gap looked dark. -->
        <item name="android:forceDarkAllowed">false</item>
    </style>
</resources>
